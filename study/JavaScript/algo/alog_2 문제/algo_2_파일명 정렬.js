// 이름만 봐도 개같을꺼 같은데 정렬이래 ㅅㅂ ㅋ
// 이걸 어떻게 짬??
// 이게 데이터가 딱딱 부위가 정해져 있으면 모르는데 아니자너
// 예제를 보면 대충 비슷비슷하긴해 아예 다른 놈들끼리 정렬하라는 소린 아닌거 같애
function solution(files) {
  let answer;
  let head;
  let number;
  let tail;

  files[0].map((e) => {});
  return answer;
}
