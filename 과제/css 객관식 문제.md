# CSS 40 문제

물론, CSS 관련 객관식 문제를 40문항 제시합니다. 답은 각 문제 뒤에 표기되어 있습니다.

### 1. CSS는 어떤 약자인가?

- a HyperText Markup Language
- b Cascading Style Sheet
- c JavaScript
- d Document Object Model

<details>
<summary>정답</summary>
정답: b Cascading Style Sheet
</details>

### 2. CSS는 어떤 역할을 하는 기술인가?

- a 웹 페이지의 구조를 정의
- b 웹 페이지의 스타일을 정의
- c 웹 페이지의 상호작용을 담당
- d 웹 페이지의 서버-클라이언트 통신을 담당

<details>
<summary>정답</summary>

정답: b 웹 페이지의 스타일을 정의

</details>

3. CSS의 'Cascading'은 무엇을 의미하는가?

   - a 폭포처럼 연결된 스타일 규칙을 적용한다.
   - b 스타일이 무효화되지 않고 연속적으로 적용된다.
   - c 선택자의 가중치에 따라 스타일 규칙이 우선순위를 갖는다.
   - d 스타일이 상속되어 하위 요소에 적용된다.

<details>
<summary>정답</summary>

정답: c 선택자의 가중치에 따라 스타일 규칙이 우선순위를 갖는다.

</details>

4. CSS에서 선택자(selector)의 역할은 무엇인가?

   - a 웹 페이지의 구조를 정의
   - b 스타일 규칙을 적용할 요소를 선택
   - c 스타일 규칙의 내용을 정의
   - d HTML 문서의 페이지 제목을 설정

<details>
<summary>정답</summary>

정답: b 스타일 규칙을 적용할 요소를 선택

</details>

5. CSS에서 사용되는 'id' 선택자는 어떤 기호로 시작하는가?

   - a .
   - b #
   - c :
   - d /

<details>
<summary>정답</summary>

정답: b #

</details>

6. CSS에서 스타일을 적용할 때, 'class' 선택자는 어떤 기호로 시작하는가?

   - a #
   - b .
   - c :
   - d \*

<details>
<summary>정답</summary>

정답: b .

</details>

7.  CSS에서 특정 요소를 선택하기 위해 사용되는 선택자는?

    - a Attribute Selector
    - b Universal Selector
    - c Descendant Selector
    - d Child Selector

<details>
<summary>정답</summary>

정답: a Attribute Selector(특성 선택자)

</details>

8. CSS에서 사용되는 'color' 속성은 무엇을 정의하는 데 사용되는가?

   - a 폰트 크기
   - b 배경 이미지
   - c 글꼴 종류
   - d 텍스트 색상
     정답: - d 텍스트 색상

9. CSS에서 글꼴 크기를 조절하는 속성은?

   - a font-weight
   - b font-family
   - c font-size
   - d text-decoration
     정답: - c font-size

10. CSS에서 박스 모델(Box Model)은 어떤 구성 요소로 구성되어 있는가?

    - a 텍스트, 이미지, 링크
    - b 마진, 패딩, 테두리, 콘텐츠
    - c 제목, 문단, 목록
    - d 폰트, 배경, 색상
      정답: - b 마진, 패딩, 테두리, 콘텐츠

11. CSS에서 박스 모델의 '마진'은 무엇을 나타내는가?

    - a 콘텐츠 영역 주변의 여백
    - b 테두리 주위의 공간
    - c 콘텐츠의 내부 여백
    - d 블록 요소의 수직 간격
      정답: - a 콘텐츠 영역 주변의 여백

12. CSS에서 박스 모델의 '패딩'은 무엇을 나타내는가?

    - a 콘텐츠 영역 주변의 여백
    - b 테두리 주위의 공간
    - c 콘텐츠의 내부 여백
    - d 블록 요소의 수직 간격
      정답: - c 콘텐츠의 내부 여백

13. CSS에서 박스 모델의 '테두리'는 어떤 역할을 하는가?
    - a 텍스트의 배경을 설정
    - b

콘텐츠 영역을 감싸고 외곽선을 생성 - c 요소 사이의 간격을 지정 - d 텍스트 색상을 변경
정답: - b 콘텐츠 영역을 감싸고 외곽선을 생성

14. CSS에서 'display' 속성을 사용하여 요소의 표시 방법을 설정할 수 있다. 'display: none;'은 어떤 효과를 가지는가?

    - a 요소를 화면에 보이지 않게 함
    - b 요소의 텍스트를 굵게 표시
    - c 요소의 배경색을 변경
    - d 요소의 크기를 확대
      정답: - a 요소를 화면에 보이지 않게 함

15. CSS에서 'float' 속성은 어떤 효과를 가지는가?

    - a 요소를 화면 중앙에 정렬
    - b 요소를 좌우로 부유시킴
    - c 요소의 높이를 자동으로 조절
    - d 요소의 가로선을 그음
      정답: - b 요소를 좌우로 부유시킴

16. CSS에서 'position' 속성을 설정할 때, 'fixed' 값은 어떤 위치에 요소를 고정시키는가?

    - a 요소를 부모 요소 내에서 고정
    - b 요소를 화면의 왼쪽 상단에 고정
    - c 요소를 스크롤에 따라 움직임
    - d 요소를 브라우저 창에 고정
      정답: - d 요소를 브라우저 창에 고정

17. CSS에서 'z-index' 속성은 무엇을 설정하는 데 사용되는가?

    - a 글꼴 크기
    - b 요소의 높이
    - c 요소의 레이어 순서
    - d 텍스트 색상
      정답: - c 요소의 레이어 순서

18. CSS에서 'background-color' 속성은 무엇을 설정하는 데 사용되는가?

    - a 텍스트 크기
    - b 배경 이미지
    - c 테두리 스타일
    - d 배경 색상
      정답: - d 배경 색상

19. CSS에서 'text-align' 속성은 어떤 내용을 정렬하는 데 사용되는가?

    - a 텍스트의 수직 정렬
    - b 요소의 가로 정렬
    - c 텍스트의 색상
    - d 텍스트의 글꼴
      정답: - b 요소의 가로 정렬

20. CSS에서 'line-height' 속성은 무엇을 설정하는 데 사용되는가?

    - a 텍스트 줄 간격
    - b 텍스트 색상
    - c 텍스트 굵기
    - d 텍스트 배경 이미지
      정답: - a 텍스트 줄 간격

21. CSS에서 'border' 속성을 사용하여 테두리를 설정할 때, 어떤 값이 설정되는가?

    - a 테두리 색상
    - b 테두리 스타일
    - c 테두리 두께
    - d 모든 위의 값
      정답: - d 모든 위의 값

22. CSS에서 'box-shadow' 속성은 무엇을 생성하는 데 사용되는가?

    - a 요소의 그림자
    - b 요소의 반투명 효과
    - c 요소의 회전 효과
    - d 요소의 확대 효과
      정답: - a 요소의 그림자

23. CSS에서 'margin' 속성은 어떤 공간을 설정하는 데 사용

되는가? - a 콘텐츠 영역 주변의 여백 - b 테두리 주위의 공간 - c 콘텐츠의 내부 여백 - d 블록 요소의 수직 간격
정답: - d 블록 요소의 수직 간격

24. CSS에서 'padding' 속성은 어떤 공간을 설정하는 데 사용되는가?

    - a 콘텐츠 영역 주변의 여백
    - b 테두리 주위의 공간
    - c 콘텐츠의 내부 여백
    - d 블록 요소의 수직 간격
      정답: - c 콘텐츠의 내부 여백

25. CSS에서 'position: relative;' 속성은 어떤 위치를 기준으로 요소를 배치하는가?

    - a 부모 요소를 기준으로 배치
    - b 화면 왼쪽 상단을 기준으로 배치
    - c 요소 자체를 기준으로 배치
    - d 브라우저 창을 기준으로 배치
      정답: - a 부모 요소를 기준으로 배치

26. CSS에서 'position: absolute;' 속성은 어떤 위치를 기준으로 요소를 배치하는가?

    - a 부모 요소를 기준으로 배치
    - b 화면 왼쪽 상단을 기준으로 배치
    - c 요소 자체를 기준으로 배치
    - d 브라우저 창을 기준으로 배치
      정답: - d 브라우저 창을 기준으로 배치

27. CSS에서 'position: fixed;' 속성은 어떤 위치를 기준으로 요소를 고정시키는가?

    - a 부모 요소를 기준으로 고정
    - b 화면 왼쪽 상단을 기준으로 고정
    - c 요소 자체를 기준으로 고정
    - d 브라우저 창을 기준으로 고정
      정답: - d 브라우저 창을 기준으로 고정

28. CSS에서 'z-index' 속성은 어떤 역할을 하는가?

    - a 텍스트 크기를 설정
    - b 요소의 높이를 조절
    - c 요소의 레이어 순서를 조절
    - d 텍스트 색상을 변경
      정답: - c 요소의 레이어 순서를 조절

29. CSS에서 'background-image' 속성은 무엇을 설정하는 데 사용되는가?

    - a 텍스트 크기
    - b 배경 이미지
    - c 테두리 스타일
    - d 배경 색상
      정답: - b 배경 이미지

30. CSS에서 'text-decoration' 속성을 사용하여 텍스트의 밑줄을 없애려면 어떤 값을 사용해야 하는가?

    - a underline
    - b overline
    - c none
    - d line-through
      정답: - c none

31. CSS에서 'float' 속성을 사용하여 요소를 왼쪽으로 부유시킬 때, 어떤 효과가 발생하는가?

    - a 요소가 화면 중앙으로 이동
    - b 요소가 왼쪽으로 이동하며 텍스트가 주위로 흐름
    - c 요소가 상단으로 이동
    - d 요소가 우측으로 이동
      정답: - b 요소가 왼쪽으로 이동하며 텍스트가 주위로 흐름

32. CSS에서 'clear' 속성은 어떤 효과를 가지는가?

    - a 요소의 투명도를 조절
    - b 요소의 크기를 조절
    - c 부모 요소에서 부유 요소의 영향을 받지 않게 함
    - d 요소의 배경 이미지를 설정
      정답: - c 부모 요소에서 부유 요소의 영향을 받지 않게 함

33. CCSS에서 'line-height' 속성은 어떤 값의 단위를 사용하여 줄 간격을 설정하는가?

    - a px
    - b em
    - c % (퍼센트)
    - d cm

      정답: a px

34. CSS에서 'border-radius' 속성은 무엇을 조절하는 데 사용되는가?

    - a 테두리 두께
    - b 테두리 색상
    - c 테두리 모서리의 둥근 정도
    - d 테두리 스타일
      정답: - c 테두리 모서리의 둥근 정도

35. CSS에서 'box-sizing' 속성은 무엇을 설정하는 데 사용되는가?

    - a 요소의 크기
    - b 요소의 내부 여백
    - c 요소의 외부 여백
    - d 요소의 상하 좌우 여백
      정답: - d 요소의 상하 좌우 여백

36. CSS에서 'text-transform' 속성은 어떤 텍스트 변환을 설정하는 데 사용되는가?

    - a 텍스트의 색상 변환
    - b 텍스트의 대문자/소문자 변환
    - c 텍스트의 배경 변환
    - d 텍스트의 글꼴 변환
      정답: - b 텍스트의 대문자/소문자 변환

37. CSS에서 'list-style' 속성은 어떤 요소에 사용되며 어떤 스타일을 설정하는가?

    - a 텍스트 요소에 사용되며 글뼴 스타일을 설정
    - b 목록 요소에 사용되며 목록 스타일을 설정
    - c 이미지 요소에 사용되며 이미지 스타일을 설정
    - d 배경 요소에 사용되며 배경 스타일을 설정
      정답: - b 목록 요소에 사용되며 목록 스타일을 설정

38. CSS에서 'font-weight' 속성은 어떤 텍스트의 속성을 설정하는 데 사용되는가?

    - a 글뼴 스타일
    - b 텍스트의 굵기
    - c 텍스트의 대소문자 변환
    - d 텍스트의 테두리 스타일
      정답: - b 텍스트의 굵기

39. CSS에서 'opacity' 속성은 무엇을 조절하는 데 사용되는가?

    - a 텍스트의 투명도
    - b 텍스트의 크기
    - c 텍스트의 색상
    - d 텍스트의 위치
      정답: - a 텍스트의 투명도

40. CSS에서 'transition' 속성은 어떤 효과를 설정하는 데 사용되는가?
    - a 애니메이션 효과
    - b 텍스트 스타일
    - c 배경 이미지
    - d 리스트 스타일
      정답: - a 애니메이션 효과
